import { ExpoConfig } from '@expo/config';
import BaseUploader, { PlatformOptions } from './BaseUploader';
export declare type AndroidPlatformOptions = PlatformOptions & {
    track: string;
    key: string;
};
export default class AndroidUploader extends BaseUploader {
    options: AndroidPlatformOptions;
    constructor(projectDir: string, options: AndroidPlatformOptions);
    _ensureExperienceIsValid(exp: ExpoConfig): void;
    _getPlatformSpecificOptions(): Promise<AndroidPlatformOptions>;
    _getServiceAccountJSONPath(): Promise<any>;
    _askForServiceAccountJSONPath(): Promise<any>;
    _uploadToTheStore(platformData: AndroidPlatformOptions, path: string): Promise<void>;
}
